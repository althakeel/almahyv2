import { OurHistoryPage, metadata as historyMetadata } from "../../our-history/page";
import type { Metadata } from "next";

export const metadata: Metadata = {
  ...historyMetadata,
  title: "Our History | Almahy Legal Services",
};

export default OurHistoryPage;
